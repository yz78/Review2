{% extends 'base.html.twig' %}

{% block title %}Review{% endblock %}

{% block body %}
<style>
    /* Couleurs principales */
        :root {
            --color-bg: #221e1f;
            --color-primary: #2f32dc;
            --color-secondary: #4525d0;
            --color-tertiary: #5c19c4;
            --color-accent: #730db8;
        }

        /* Styles de base */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: var(--color-bg);
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: var(--color-primary);
            font-size: 12em;
            margin-bottom: 20px;
        }

        p {
            color: var(--color-tertiary);
            font-size: 1.3em;
            text-align: center;
            max-width: 600px;
        }

        

        .cta-button {
            background-color: var(--color-secondary);
            color: #fff;
            padding: 10px 20px;
            font-size: 1.4em;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            margin-top: 20px;
        }

        .cta-button:hover {
            background-color: var(--color-accent);
        }

        /* Barre de recherche dans la navbar */
        .form-inline .form-control {
            width: 400px;
            margin-right: 20px;
            display: flex;
            align-items: center; 
            margin-right: 1px;
        }

        /* Bouton Rechercher */
.btn-outline-light {
    transition: background-color 0.3s, border-color 0.3s, color 0.3s;
    border-color: var(--color-primary);
    color: var(--color-primary);
    animation: colorChange 5s infinite; /* Animation de 5 secondes, en boucle infinie */
}

/* Animation des couleurs */
@keyframes colorChange {
    0% {
        background-color: var(--color-primary); /* Couleur initiale */
        border-color: var(--color-primary);
        color: #ffffff;
    }
    33% {
        background-color: var(--color-secondary); /* Deuxième couleur */
        border-color: var(--color-secondary);
        color: #ffffff;
    }
    66% {
        background-color: var(--color-accent); /* Troisième couleur */
        border-color: var(--color-accent);
        color: #ffffff;
    }
    100% {
        background-color: var(--color-primary); /* Retour à la couleur initiale */
        border-color: var(--color-primary);
        color: #ffffff;
    }
}


         

        .navbar-spacing {
    height: 150px; /* Ajuste cette valeur selon la hauteur de la navbar */
}
</style>

<body>
<div class="navbar-spacing"></div>
   <form class="form-inline my-1 my-lg-0">
                <input class="form-control mr-sm-1" type="search" placeholder="Rechercher" aria-label="Search">
                <button class="btn btn-outline-light my-2 my-sm-0" type="submit">Rechercher</button>
    </form>
 <h1><img src="{{ asset('image/logo.png') }}" alt="Logo Review Original"></h1>

    
    <p>Découvrez les critiques et avis les plus détaillés sur vos films et séries préférés.</p>
    <a href="#" class="cta-button">Commencer à explorer</a>
</body>
{% endblock %}

